<!DOCTYPE html>
<html>

    <head>
        <meta charset="UTF-8">
        <title>Projet réservations - {% block title %}{% endblock %}</title>
        <link rel="stylesheet" href="/css/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>

    <body>


        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="{{path('home')}}">Réservation</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{path('home')}}">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('artist')}}">Artistes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('show')}}">Shows</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('representation')}}">Représentations</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('location')}}">Location</a>
                    </li>

                    {% if not app.user %}
                    <li class="nav navbar-nav navbar-right" style="left:0px">
                        <a class="nav-link" href="{{ path('security_login')}}">Connexion</a>
                    </li><li class="nav navbar-nav navbar-right" style="left:0px">
                        <a class="nav-link" href="{{ path('security_registration')}}">Inscription</a>
                    </li>
                    {% else %}

                    <li class="nav navbar-nav navbar-right">
                        <a class="nav-link" href="{{ path('security_logout')}}">Déconnexion</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </nav>
        <div class="container mt-5">
            {% block body %}{% endblock %}
        </div>
        <script src="/js/jquery.min.js"></script>
        <script src="/js/popper.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>

</html>